# DESIGN.md — Project Design Contract

**Status:** Active — all agents MUST comply  
**Source:** `DESIGN.md.template` (this file — copy to `DESIGN.md`, fill project values)

---

## 1. PROJECT IDENTITY & DESIGN PHILOSOPHY

### 1.1 Project Purpose
**{{PROJECT_PURPOSE}}** — problem solved? who uses it?

### 1.2 Primary Audience
**{{PRIMARY_AUDIENCE}}**

### 1.3 Primary Conversion Goal
**{{PRIMARY_GOAL}}** — one action visitor must take

### 1.4 Design Philosophy (mandated by `frontend-design`)

| Principle | Project Decision |
|---|---|
| **Aesthetic Direction** | {{AESTHETIC_DIRECTION}} |
| **Typography** | {{FONT_DISPLAY}} (display) + {{FONT_BODY}} (body) |
| **Color Strategy** | {{COLOR_STRATEGY}} |
| **Layout Approach** | {{LAYOUT_APPROACH}} |
| **Background Treatment** | {{BACKGROUND_TREATMENT}} |
| **Motion Choreography** | {{MOTION_CHOREOGRAPHY}} |
| **Differentiator** | {{DIFFERENTIATION}} — one thing visitors remember |

---

## 2. DESIGN TOKENS

Fill these into `src/styles/boilerplate/variables.css`. Each category maps to CSS custom properties in that file.

### 2.1 Color Palette

| Token | Light Value | Dark Value | Usage |
|-------|-------------|------------|-------|
| `--color-bg` | {{hex}} | {{hex}} | Page background |
| `--color-bg-alt` | {{hex}} | {{hex}} | Alternate sections |
| `--color-bg-elevated` | {{hex}} | {{hex}} | Cards, modals |
| `--color-text` | {{hex}} | {{hex}} | Primary text |
| `--color-text-muted` | {{hex}} | {{hex}} | Secondary text |
| `--color-text-inverse` | {{hex}} | {{hex}} | Text on dark backgrounds |
| `--color-primary` | {{hex}} | {{hex}} | Brand dominant |
| `--color-primary-hover` | {{hex}} | {{hex}} | Brand hover state |
| `--color-primary-active` | {{hex}} | {{hex}} | Brand active state |
| `--color-accent` | {{hex}} | {{hex}} | CTAs, links |
| `--color-accent-hover` | {{hex}} | {{hex}} | Accent hover |
| `--color-border` | {{hex}} | {{hex}} | Subtle borders |
| `--color-border-strong` | {{hex}} | {{hex}} | Emphasized borders |
| `--color-focus` | {{hex}} | {{hex}} | Focus rings |
| `--color-error` | {{hex}} | — | Error states |
| `--color-success` | {{hex}} | — | Success states |
| `--color-warning` | {{hex}} | — | Warning states |

**Dark mode:** copy color tokens into `@media (prefers-color-scheme: dark) { :root { ... } }`. Override only color tokens.

### 2.2 Typography Scale (use `clamp()` for fluid sizing)

| Token | Value | Font Family |
|-------|-------|-------------|
| `--font-display` | — | {{font-name}} |
| `--font-body` | — | {{font-name}} |
| `--font-mono` | — | {{font-name}} |
| `--font-size-fluid-base` | {{clamp()}} | Base for body |
| `--font-size-display-1` | {{clamp()}} | Largest heading |
| `--font-size-display-2` | {{clamp()}} | |
| `--font-size-display-3` | {{clamp()}} | |
| `--font-size-heading-1` | {{clamp()}} | |
| `--font-size-heading-2` | {{clamp()}} | |
| `--font-size-heading-3` | {{clamp()}} | |
| `--font-size-body` | `var(--font-size-fluid-base)` | |
| `--font-size-body-sm` | {{clamp()}} | |
| `--font-size-caption` | {{clamp()}} | |
| `--line-height-tight` | {{value}} | Headings |
| `--line-height-base` | {{value}} | Body |
| `--line-height-relaxed` | {{value}} | Long-form |
| `--letter-spacing-tight` | {{value}} | |
| `--letter-spacing-normal` | {{value}} | |
| `--letter-spacing-wide` | {{value}} | |

**Font files:** place TTF/WOFF2 in `src/assets/fonts/`. Declare `@font-face` in `boilerplate/fonts.css` with relative CSS paths. No Google Fonts CDN.

### 2.3 Spacing (8px base scale)

`--space-1` through `--space-10` = 0.25rem through 8rem (step). Defaults in `variables.css`. Override only if project needs non-8px rhythm.

| Token | Default |
|-------|---------|
| `--space-1` | `0.25rem` |
| `--space-2` | `0.5rem` |
| `--space-3` | `0.75rem` |
| `--space-4` | `1rem` |
| `--space-5` | `1.5rem` |
| `--space-6` | `2rem` |
| `--space-7` | `3rem` |
| `--space-8` | `4rem` |
| `--space-9` | `6rem` |
| `--space-10` | `8rem` |

### 2.4 Section Dimensions

| Token | Value |
|-------|-------|
| `--container-max` | {{value}} |
| `--container-padding` | {{value}} |
| `--header-height` | {{value}} |
| `--footer-height` | {{value}} |

### 2.5 Motion (defaults in `variables.css` — override per project)

| Token | Default | Use |
|-------|---------|-----|
| `--duration-fast` | `150ms` | Micro-interactions |
| `--duration-base` | `250ms` | Standard transitions |
| `--duration-slow` | `400ms` | Page transitions |
| `--duration-slower` | `600ms` | Hero reveals |
| `--ease-standard` | `cubic-bezier(0.4, 0, 0.2, 1)` | UI transitions |
| `--ease-expressive` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | Entrances, emphasis |
| `--ease-spring` | `cubic-bezier(0.175, 0.885, 0.32, 1.275)` | Bouncy micro-interactions |

### 2.6 Z-Index Scale (defaults — override if conflicts arise)

| Token | Default | Usage |
|-------|---------|-------|
| `--z-base` | `1` | Base layer |
| `--z-dropdown` | `100` | Dropdown menus |
| `--z-sticky` | `200` | Sticky headers |
| `--z-modal-backdrop` | `300` | Modal backdrops |
| `--z-modal` | `400` | Modal dialogs |
| `--z-toast` | `500` | Toast notifications |
| `--z-tooltip` | `600` | Tooltips |

---

## 3. COMPONENT INVENTORY

| Component | Status | CSS File | JS Module |
|-----------|--------|----------|-----------|
| | ☐ Planned | | |
| | ☐ Planned | | |
| | ☐ Planned | | |
| | ☐ Planned | | |

---

## 4. ENFORCEMENT

Binding contract. `@code-review` rejects violations. Propose changes: `@orchestrator` → user approval → update.

---

*End of DESIGN.md.template — copy to DESIGN.md and fill project values.*
